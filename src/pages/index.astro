---
import Layout from '@/layouts/Layout.astro'
import { defaultLang, useTranslations } from '@/lang'

const t = useTranslations(defaultLang)
---

<Layout lang={defaultLang}>
  <main>content - {t.title}</main>
</Layout>
